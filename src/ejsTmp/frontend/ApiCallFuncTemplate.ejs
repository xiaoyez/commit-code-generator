<% if (api.comment) {
%>/** <%= api.comment %> */
<% }
%>export function <%= api.apiName %>(<% if (api.params) {
%><%= api.params.name %>: <%= tsTypeString(api.params.type) %><%
} %>) {
    return request<<%=
    api.result ? tsTypeString(api.result) : 'void'
    %>>({
        url: "<%= baseUrl + api.url %>",
        method: "<%= api.method %>",<% if (api.params) {%>
        <%=
        api.method === 'get' ? 'params' : 'data'
        %>: <%= api.params.name %><% } %>
    });
}